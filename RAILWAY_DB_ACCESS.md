# 🔌 Подключение к PostgreSQL на Railway с локальной машины

## Проблема

Railway использует два типа URL для подключения к PostgreSQL:

1. **`DATABASE_URL`** - внутренний URL (`postgres.railway.internal`)
   - Работает только внутри сети Railway
   - Используется ботом на Railway
   - **НЕ работает** с вашего компьютера

2. **`DATABASE_PUBLIC_URL`** - публичный URL
   - Работает из любой точки интернета
   - Используется для внешних подключений
   - **Работает** с вашего компьютера

## Решение: Использовать DATABASE_PUBLIC_URL

### Шаг 1: Получите DATABASE_PUBLIC_URL на Railway

1. Откройте ваш проект на Railway: https://railway.app/dashboard
2. Выберите сервис **Postgres**
3. Перейдите в раздел **Variables**
4. Найдите переменную **`DATABASE_PUBLIC_URL`**
5. Скопируйте её значение

### Шаг 2: Добавьте в .env файл

Откройте `.env` файл и добавьте:

```bash
DATABASE_PUBLIC_URL=postgresql://user:password@host.railway.app:port/railway
```

**Важно:** Используйте `DATABASE_PUBLIC_URL`, а не `DATABASE_URL` для локальных скриптов!

### Шаг 3: Используйте скрипты

Теперь скрипты `view_user_data.py` и `view_analytics.py` смогут подключиться к облачной базе данных.

## Альтернативные способы

### Вариант 1: Railway CLI

1. Установите Railway CLI:
   ```bash
   npm i -g @railway/cli
   ```

2. Войдите:
   ```bash
   railway login
   ```

3. Подключитесь к проекту:
   ```bash
   railway link
   ```

4. Используйте переменные окружения Railway:
   ```bash
   railway run python view_user_data.py --list
   ```

### Вариант 2: Просмотр через Railway Dashboard

1. Откройте PostgreSQL сервис на Railway
2. Перейдите в раздел **Database** → **Data**
3. Просматривайте таблицы напрямую в интерфейсе

### Вариант 3: Использовать pgAdmin или другой клиент

1. Получите `DATABASE_PUBLIC_URL` из Railway
2. Подключитесь через любой PostgreSQL клиент:
   - pgAdmin
   - DBeaver
   - TablePlus
   - и т.д.

## Безопасность

⚠️ **Важно:** `DATABASE_PUBLIC_URL` доступен из интернета. 

- Не коммитьте его в git
- Храните только в `.env` файле
- Добавьте `.env` в `.gitignore`

## Проверка подключения

После добавления `DATABASE_PUBLIC_URL` в `.env`:

```bash
python view_user_data.py --list
```

Должно появиться:
```
🔌 Подключение к PostgreSQL: host.railway.app:port/railway
✅ Подключение к PostgreSQL установлено
```

---

**Готово!** Теперь вы можете анализировать пользователей из облачной базы данных с локальной машины.

