# Развертывание бота на Timeweb Cloud через Docker 🐳

Инструкция по развертыванию Astral Bot на Timeweb Cloud с использованием Docker и встроенной PostgreSQL.

## 📋 Что вам понадобится

1. Аккаунт на Timeweb Cloud (https://timeweb.cloud)
2. Созданный контейнер в Timeweb Cloud
3. Подключенная база данных PostgreSQL
4. Токен бота от @BotFather

## 🚀 Шаг 1: Подготовка в Timeweb Cloud

### 1.1 Создание контейнера

1. Зайдите на https://timeweb.cloud
2. Перейдите в раздел **"Контейнеры"** → **"Создать контейнер"**
3. Выберите:
   - **Тип**: Docker контейнер
   - **Образ**: Укажите образ позже или оставьте пустым
   - **Регион**: Москва или Санкт-Петербург
   - **Тариф**: Минимум 512 MB RAM, 1 GB диск

### 1.2 Создание базы данных PostgreSQL

1. В панели Timeweb Cloud перейдите в **"Базы данных"** → **"Создать базу"**
2. Выберите **PostgreSQL**
3. Выберите тариф (минимум 512 MB)
4. Сохраните данные подключения:
   - Хост
   - Порт
   - Имя базы данных
   - Пользователь
   - Пароль

## 🔧 Шаг 2: Подключение репозитория

### Вариант A: Через Git (рекомендуется)

1. В настройках контейнера выберите **"Git"**
2. Укажите URL репозитория: `https://github.com/NikRoshchin1894/astral-bot.git`
3. Выберите ветку: `main`
4. Укажите путь к Dockerfile: `./Dockerfile`

### Вариант B: Через Docker Registry

Если у вас есть доступ к Docker Registry, можно использовать образ напрямую.

## ⚙️ Шаг 3: Настройка переменных окружения

В настройках контейнера в разделе **"Переменные окружения"** добавьте:

```env
# Токен бота (ОБЯЗАТЕЛЬНО)
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather

# OpenAI API ключ
OPENAI_API_KEY=ваш_openai_api_key

# ЮKassa настройки
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key

# Username бота (без @)
TELEGRAM_BOT_USERNAME=ваш_bot_username

# URL для возврата после оплаты
PAYMENT_SUCCESS_URL=https://t.me/ваш_bot_username?start=payment_success
PAYMENT_RETURN_URL=https://t.me/ваш_bot_username?start=payment_cancel

# База данных PostgreSQL из Timeweb Cloud
DATABASE_PUBLIC_URL=postgresql://пользователь:пароль@хост:порт/имя_базы

# Webhook (опционально, если есть домен)
# YOOKASSA_WEBHOOK_URL=https://ваш_домен.ru/webhook/yookassa
# WEBHOOK_PORT=8080
```

**Важно**: Замените все значения на ваши реальные данные!

## 🗄️ Шаг 4: Формирование строки подключения к базе данных

Строка подключения должна выглядеть так:

```
postgresql://username:password@host:port/database_name
```

Пример:
```
postgresql://astral_user:MyPassword123@pg-abc123.timeweb.cloud:5432/astral_bot
```

## 🌐 Шаг 5: Настройка портов

В настройках контейнера:

1. Откройте раздел **"Порты"**
2. Добавьте порт:
   - **Внешний порт**: 8080 (или любой свободный)
   - **Внутренний порт**: 8080
   - **Протокол**: TCP

Этот порт нужен для webhook от ЮKassa (если будете использовать).

## 📦 Шаг 6: Настройка volumes (томов)

В Timeweb Cloud обычно не требуется настраивать volumes для SQLite, так как используется PostgreSQL.

Если хотите сохранять логи на постоянном диске:

1. В настройках контейнера перейдите в **"Volumes"**
2. Создайте volume для логов:
   - **Путь в контейнере**: `/app/logs`
   - **Путь на хосте**: выберите из списка или создайте новый

## 🚀 Шаг 7: Запуск контейнера

1. Сохраните все настройки
2. Нажмите **"Развернуть"** или **"Запустить"**
3. Дождитесь завершения сборки и запуска (может занять 5-10 минут)

## ✅ Шаг 8: Проверка работы

### Просмотр логов

1. В панели контейнера перейдите в раздел **"Логи"**
2. Должны появиться сообщения:
   ```
   ✅ База данных найдена
   ✅ Подключение к PostgreSQL установлено
   Бот запущен и готов к работе
   ```

### Тест бота

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Бот должен ответить приветственным сообщением

## 🔄 Управление контейнером

### Перезапуск

1. В панели контейнера нажмите **"Перезапустить"**
2. Или остановите и запустите заново

### Обновление бота

1. Если используете Git:
   - Нажмите **"Обновить из Git"** в настройках контейнера
   - Или контейнер может обновляться автоматически

2. Если используете Docker образ:
   - Пересоберите образ
   - Перезапустите контейнер

### Просмотр логов

В панели контейнера:
- **Логи** → Просмотр в реальном времени
- **История логов** → Просмотр старых логов

## 🌐 Шаг 9: Настройка домена для webhook (опционально)

Если хотите использовать webhook для мгновенных уведомлений от ЮKassa:

### 9.1 Получение домена

1. В Timeweb Cloud перейдите в **"Домены"**
2. Добавьте или подключите домен
3. Настройте DNS:
   - Тип: A
   - Имя: @ (или поддомен, например `bot`)
   - Значение: IP-адрес вашего контейнера

### 9.2 Настройка Nginx/прокси

Timeweb Cloud обычно предоставляет встроенный прокси или Nginx.

1. В настройках контейнера найдите раздел **"Домены"** или **"Прокси"**
2. Привяжите ваш домен к контейнеру
3. Настройте проксирование для `/webhook/yookassa` → `localhost:8080`

### 9.3 Обновление переменных окружения

Добавьте в переменные окружения:

```env
YOOKASSA_WEBHOOK_URL=https://ваш_домен.ru/webhook/yookassa
WEBHOOK_PORT=8080
```

### 9.4 Настройка в ЮKassa

1. Зайдите в личный кабинет ЮKassa: https://yookassa.ru/my
2. **Настройки** → **Уведомления**
3. Добавьте URL: `https://ваш_домен.ru/webhook/yookassa`
4. Сохраните

## 🐛 Решение проблем

### Контейнер не запускается

1. **Проверьте логи** в панели Timeweb Cloud
2. **Проверьте переменные окружения** - все ли установлены
3. **Проверьте подключение к базе данных**:
   - Убедитесь, что строка `DATABASE_PUBLIC_URL` правильная
   - Проверьте, что база данных запущена

### Бот не отвечает в Telegram

1. Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
2. Проверьте логи на ошибки
3. Убедитесь, что контейнер запущен

### Ошибки подключения к базе данных

1. Проверьте строку подключения `DATABASE_PUBLIC_URL`
2. Убедитесь, что база данных PostgreSQL запущена в Timeweb Cloud
3. Проверьте, что хост и порт правильные
4. Проверьте имя пользователя и пароль

### Проблемы с webhook

1. Убедитесь, что порт 8080 открыт и проксируется
2. Проверьте, что домен правильно настроен
3. Проверьте логи на ошибки Flask сервера

## 📊 Мониторинг

### Использование ресурсов

В панели контейнера вы можете видеть:
- Использование CPU
- Использование памяти (RAM)
- Использование диска
- Сетевая активность

### Логи

- **Живые логи**: Просмотр в реальном времени
- **История**: Просмотр старых логов
- **Экспорт**: Скачивание логов для анализа

## 💰 Стоимость

Примерная стоимость на Timeweb Cloud:

- **Контейнер**: от 200₽/мес (512 MB RAM)
- **PostgreSQL**: от 150₽/мес (512 MB)
- **Домен**: от 149₽/год (опционально)
- **Итого**: от 350₽/мес без домена

## 📞 Поддержка

- **Timeweb Cloud**: https://timeweb.cloud/help (техническая поддержка 24/7)
- **Документация Timeweb Cloud**: https://timeweb.cloud/docs

---

**Готово!** Ваш бот должен работать на Timeweb Cloud! 🎉

Если возникли проблемы - проверьте логи и убедитесь, что все переменные окружения установлены корректно.

