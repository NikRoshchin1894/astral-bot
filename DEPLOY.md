# Быстрый деплой бота 🚀

## Быстрый старт (5 минут)

### Вариант 1: Railway.app (Самый простой) ⭐

1. Зайдите на https://railway.app и войдите через GitHub
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Подключите этот репозиторий
4. В настройках проекта добавьте переменную:
   - Key: `TELEGRAM_BOT_TOKEN`
   - Value: ваш токен от @BotFather
5. Готово! Бот автоматически запустится

### Вариант 2: Render.com (Бесплатный)

1. Зайдите на https://render.com и войдите
2. Нажмите "New +" → "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - Name: `astral-bot` (любое имя)
   - Region: выбирайте ближайший
   - Branch: `main` или `master`
   - Root Directory: `.` (точка)
   - Environment: `Python 3`
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python bot.py`
5. В разделе "Environment Variables" добавьте:
   - Key: `TELEGRAM_BOT_TOKEN`
   - Value: ваш токен
6. Нажмите "Create Web Service"
7. Готово!

### Вариант 3: PythonAnywhere (Очень просто)

1. Зарегистрируйтесь на https://www.pythonanywhere.com (бесплатный аккаунт)
2. Откройте "Consoles" → "Bash"
3. Выполните:
   ```bash
   git clone <ваш-репозиторий-url>
   cd Astral_Bot
   pip3.10 install --user -r requirements.txt
   ```
4. Создайте файл `env.py`:
   ```bash
   nano env.py
   ```
   Добавьте:
   ```python
   import os
   os.environ['TELEGRAM_BOT_TOKEN'] = 'ваш_токен'
   ```
5. Откройте `bot.py` и добавьте в начало файла:
   ```python
   import env
   ```
   (после строк `# -*- coding: utf-8 -*-` и комментария)
6. В веб-интерфейсе PythonAnywhere:
   - "Tasks" → "Schedule a new task"
   - Command: `python3.10 /home/ваше_имя/Astral_Bot/bot.py`
   - Minute: любая (например, 0)
   - Hour: любая (например, 0)
   - Click "Create"
7. Готово!

### Вариант 4: DigitalOcean (Требует больше опыта)

Полные инструкции в README.md, раздел "Вариант 1: DigitalOcean Droplet"

## Получение токенов

**Telegram Bot Token:**
1. Откройте Telegram
2. Найдите бота @BotFather
3. Отправьте `/newbot`
4. Следуйте инструкциям
5. Скопируйте токен (выглядит как: `123456789:ABCdef...`)

**OpenAI API Key (опционально, для натальной карты):**
1. Зарегистрируйтесь на https://platform.openai.com/
2. Получите API ключ в разделе API Keys
3. Скопируйте ключ (выглядит как: `sk-...`)

## Проверка работы

После деплоя:
1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Бот должен ответить приветствием с кнопками

## Решение проблем

**Бот не отвечает:**
- Проверьте логи на платформе
- Убедитесь, что токен правильный
- Убедитесь, что бот запущен

**Ошибка "ModuleNotFoundError":**
```bash
pip install -r requirements.txt
```

**Бот падает при запуске:**
- Проверьте, что переменная окружения `TELEGRAM_BOT_TOKEN` установлена
- Проверьте логи на платформе

## Контакты

Если возникли проблемы - создайте Issue в репозитории.
