# Подключение базы данных PostgreSQL к боту на Timeweb Cloud 📊

Инструкция по подключению базы данных PostgreSQL из Timeweb Cloud к вашему боту.

## 🔍 Шаг 1: Создание базы данных в Timeweb Cloud

Если база данных еще не создана:

1. Зайдите в панель Timeweb Cloud: https://timeweb.cloud
2. Перейдите в раздел **"Базы данных"** → **"Создать базу"**
3. Выберите **PostgreSQL**
4. Выберите тариф (минимум 512 MB для начала)
5. Дождитесь создания базы данных (обычно 1-2 минуты)

## 📋 Шаг 2: Получение данных подключения

После создания базы данных:

1. В списке баз данных найдите вашу базу PostgreSQL
2. Нажмите на неё, чтобы открыть детали
3. Скопируйте следующие данные:
   - **Хост** (Host) - например: `pg-abc123.timeweb.cloud`
   - **Порт** (Port) - обычно `5432`
   - **Имя базы данных** (Database) - например: `astral_bot`
   - **Пользователь** (User) - например: `astral_user`
   - **Пароль** (Password) - скопируйте пароль (он показывается один раз!)

## 🔗 Шаг 3: Формирование строки подключения

Строка подключения должна иметь формат:

```
postgresql://пользователь:пароль@хост:порт/имя_базы
```

**Пример:**
```
postgresql://astral_user:MyPassword123@pg-abc123.timeweb.cloud:5432/astral_bot
```

**Важно:**
- Если в пароле есть специальные символы (например, `@`, `#`, `:`), их нужно закодировать в URL:
  - `@` → `%40`
  - `#` → `%23`
  - `:` → `%3A`
  - `/` → `%2F`
  - `?` → `%3F`
  - `&` → `%26`
  - `=` → `%3D`

**Пример с закодированным паролем:**
Если пароль `P@ss#123`, то строка будет:
```
postgresql://astral_user:P%40ss%23123@pg-abc123.timeweb.cloud:5432/astral_bot
```

## ⚙️ Шаг 4: Настройка переменной окружения в контейнере

1. В панели Timeweb Cloud откройте ваш **контейнер с ботом**
2. Перейдите в раздел **"Переменные окружения"** (Environment Variables)
3. Найдите переменную `DATABASE_PUBLIC_URL` или создайте новую:
   
   **Ключ (Key):** `DATABASE_PUBLIC_URL`
   
   **Значение (Value):** Вставьте строку подключения из шага 3
   
   Например:
   ```
   postgresql://astral_user:MyPassword123@pg-abc123.timeweb.cloud:5432/astral_bot
   ```

4. **Сохраните изменения**

## 🔄 Шаг 5: Перезапуск контейнера

1. После добавления переменной окружения **перезапустите контейнер**
2. В панели контейнера нажмите **"Перезапустить"** или **"Пересоздать"**

## ✅ Шаг 6: Проверка подключения

### Через логи контейнера:

1. Откройте раздел **"Логи"** в панели контейнера
2. Ищите следующие сообщения при успешном подключении:

```
✅ База данных найдена (первые 20 символов: postgresql://...)
Используется DATABASE_PUBLIC_URL
Подключение к PostgreSQL: хост:порт/имя_базы
✅ Подключение к PostgreSQL установлено
✅ База данных инициализирована (postgresql)
```

### Если видите ошибки:

```
❌ Ошибка подключения к PostgreSQL: ...
```

То проверьте:
- Правильность строки подключения
- Правильность кодировки специальных символов в пароле
- Что база данных запущена в Timeweb Cloud
- Что хост и порт правильные

## 🔧 Шаг 7: Инициализация таблиц

Бот автоматически создаст необходимые таблицы при первом запуске. Но если нужно проверить вручную:

1. В панели Timeweb Cloud откройте вашу базу данных PostgreSQL
2. Перейдите в раздел **"Веб-консоль"** или **"phpPgAdmin"** (если доступно)
3. Выполните SQL-запрос для проверки таблиц:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public';
```

Должны быть созданы таблицы:
- `users` - пользователи бота
- `events` - события для аналитики
- `payments` - платежи

## 🐛 Решение проблем

### Ошибка: "connection refused" или "could not connect"

**Причины:**
- Неверный хост или порт
- База данных не запущена
- Firewall блокирует подключение

**Решение:**
1. Проверьте, что база данных запущена в Timeweb Cloud
2. Убедитесь, что хост и порт правильные
3. Проверьте, что контейнер и база данных в одном регионе/сети

### Ошибка: "authentication failed"

**Причины:**
- Неверное имя пользователя или пароль
- Пароль содержит специальные символы, которые не закодированы

**Решение:**
1. Проверьте пароль в настройках базы данных Timeweb Cloud
2. Закодируйте специальные символы в пароле (см. шаг 3)
3. Пересоздайте строку подключения

### Ошибка: "database does not exist"

**Причины:**
- Неверное имя базы данных в строке подключения

**Решение:**
1. Проверьте точное имя базы данных в панели Timeweb Cloud
2. Убедитесь, что имя базы совпадает в строке подключения

### База подключается, но используется SQLite

**Причина:**
- Переменная `DATABASE_PUBLIC_URL` не установлена или установлена неправильно

**Решение:**
1. Проверьте, что переменная `DATABASE_PUBLIC_URL` добавлена в настройках контейнера
2. Проверьте, что значение переменной правильное (строка подключения)
3. Убедитесь, что нет пробелов или лишних символов
4. Перезапустите контейнер после изменения переменных

## 📝 Пример полной настройки переменных окружения

В настройках контейнера должны быть установлены все эти переменные:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
OPENAI_API_KEY=sk-...
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key
TELEGRAM_BOT_USERNAME=ваш_bot_username
PAYMENT_SUCCESS_URL=https://t.me/ваш_bot_username?start=payment_success
PAYMENT_RETURN_URL=https://t.me/ваш_bot_username?start=payment_cancel
DATABASE_PUBLIC_URL=postgresql://пользователь:пароль@хост:порт/имя_базы
```

## ✅ Чеклист подключения

- [ ] База данных PostgreSQL создана в Timeweb Cloud
- [ ] Получены все данные подключения (хост, порт, пользователь, пароль, имя БД)
- [ ] Сформирована строка подключения в правильном формате
- [ ] Специальные символы в пароле закодированы (если есть)
- [ ] Переменная `DATABASE_PUBLIC_URL` добавлена в настройках контейнера
- [ ] Контейнер перезапущен после добавления переменной
- [ ] В логах видны сообщения об успешном подключении к PostgreSQL
- [ ] Таблицы созданы автоматически при первом запуске

## 💡 Полезные ссылки

- **Timeweb Cloud**: https://timeweb.cloud
- **Панель управления**: https://timeweb.cloud/dashboard
- **Документация PostgreSQL**: https://www.postgresql.org/docs/

---

После выполнения всех шагов база данных должна быть подключена, и бот будет использовать PostgreSQL вместо SQLite! 🎉

