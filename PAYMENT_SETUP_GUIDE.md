# 💳 Руководство по настройке оплаты и выдачи натальной карты

## 📋 Как работает процесс оплаты

1. **Пользователь нажимает "Оплатить"** в боте
2. **YooKassa создает платеж** и возвращает ссылку на оплату
3. **Пользователь оплачивает** на странице YooKassa
4. **YooKassa отправляет webhook** на ваш сервер (`/webhook/yookassa`)
5. **Бот обрабатывает платеж** и автоматически генерирует натальную карту
6. **Пользователь получает натальную карту** в боте

## ❌ Текущие проблемы

### 1. Webhook недоступен (502 Bad Gateway)
- **Проблема**: YooKassa не может отправить уведомление о платеже на ваш сервер
- **Причина**: Бот не запущен или webhook сервер недоступен
- **Решение**: Запустить бот на сервере (см. ниже)

### 2. Резервные механизмы
Бот имеет резервные механизмы, которые работают даже без webhook:
- ✅ Периодическая проверка платежей (каждые 2 минуты)
- ✅ Проверка при возврате пользователя (`/start payment_success`)

**НО**: Webhook быстрее и надежнее!

## ✅ Пошаговая настройка

### Шаг 1: Проверьте настройки YooKassa

Убедитесь, что в `.env` файле (или переменных окружения на сервере) установлены:

```bash
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key
YOOKASSA_WEBHOOK_URL=https://nikroshchin1894-astral-bot-e77d.twc1.net/webhook/yookassa
```

**Важно**: 
- URL должен быть HTTPS
- URL должен заканчиваться на `/webhook/yookassa`

### Шаг 2: Запустите бот на сервере

Подключитесь к вашему серверу Timeweb Cloud и проверьте:

```bash
# Проверьте, запущен ли бот
ps aux | grep bot.py

# Или через systemd (если используется)
sudo systemctl status astral-bot
```

Если бот не запущен, запустите его:

```bash
# Вариант 1: Через systemd
sudo systemctl start astral-bot
sudo systemctl enable astral-bot

# Вариант 2: Вручную (в screen/tmux)
cd /path/to/Astral_Bot
source venv/bin/activate
python bot.py
```

### Шаг 3: Проверьте логи бота

После запуска бота должны появиться логи:

```
🌐 Запуск webhook сервера на 0.0.0.0:8080
   📱 Telegram webhook: /webhook/telegram
   💳 YooKassa webhook: /webhook/yookassa
```

Если этих логов нет - webhook сервер не запустился!

### Шаг 4: Проверьте доступность webhook

Выполните проверку (локально или на сервере):

```bash
python check_all_webhooks.py
```

Должно быть:
- ✅ YooKassa webhook: Работает корректно (HTTP 200)
- ✅ Telegram webhook: Работает корректно

### Шаг 5: Зарегистрируйте webhook в YooKassa

1. Войдите в [личный кабинет YooKassa](https://yookassa.ru/my)
2. Перейдите в **Настройки → Уведомления**
3. Добавьте webhook URL:
   ```
   https://nikroshchin1894-astral-bot-e77d.twc1.net/webhook/yookassa
   ```
4. Выберите события:
   - ✅ `payment.succeeded` (обязательно!)
   - ✅ `payment.canceled` (рекомендуется)

### Шаг 6: Проверьте настройки прокси в Timeweb Cloud

Убедитесь, что в настройках вашего приложения:

- Запросы к `/webhook/yookassa` проксируются на `localhost:8080`
- Порт 8080 открыт и доступен

**Настройка Nginx/прокси:**
```nginx
location /webhook/yookassa {
    proxy_pass http://localhost:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

### Шаг 7: Протестируйте оплату

1. Откройте бота в Telegram
2. Нажмите "Оплатить" (или выберите натальную карту)
3. Оплатите тестовой картой:
   - **Тестовая карта**: `5555 5555 5555 4444`
   - **CVV**: любой (например, `123`)
   - **Срок**: любой будущий месяц/год
   - **3D Secure**: `12345`
4. После оплаты бот должен автоматически:
   - Пометить вас как оплатившего
   - Сгенерировать натальную карту (если профиль заполнен)
   - Или предложить заполнить профиль

## 🔍 Диагностика проблем

### Проблема: Webhook возвращает 502

**Решение:**
1. Проверьте, запущен ли бот: `ps aux | grep bot.py`
2. Проверьте логи бота на ошибки
3. Проверьте, что порт 8080 открыт: `netstat -tuln | grep 8080`
4. Проверьте настройки прокси/маршрутизации

### Проблема: Платеж прошел, но натальная карта не генерируется

**Решение:**
1. Проверьте, заполнен ли профиль пользователя:
   - Имя
   - Дата рождения
   - Время рождения
   - Место рождения
2. Проверьте логи бота на ошибки генерации
3. Проверьте, что OpenAI API ключ установлен и работает
4. Используйте резервную проверку: `/start payment_success`

### Проблема: Платеж не обрабатывается автоматически

**Решение:**
1. Проверьте, что webhook зарегистрирован в YooKassa
2. Проверьте, что webhook доступен (не 502)
3. Проверьте логи бота на наличие ошибок обработки
4. Используйте резервную проверку:
   - Подождите 2 минуты (периодическая проверка)
   - Или используйте `/start payment_success`

## 🛠 Полезные скрипты

```bash
# Проверка всех webhook
python check_all_webhooks.py

# Детальная диагностика YooKassa webhook
python diagnose_webhook.py

# Проверка статуса бота
python check_bot_status.py

# Проверка платежа вручную
python process_successful_payment.py <payment_id> [user_id]

# Синхронизация статусов платежей
python sync_payment_statuses.py
```

## ✅ Чек-лист настройки

- [ ] YooKassa credentials установлены (Shop ID, Secret Key)
- [ ] YOOKASSA_WEBHOOK_URL установлен и правильный
- [ ] Бот запущен на сервере
- [ ] Webhook сервер запустился (логи показывают "🌐 Запуск webhook сервера")
- [ ] Webhook доступен (не 502)
- [ ] Webhook зарегистрирован в личном кабинете YooKassa
- [ ] Прокси настроен правильно (если используется)
- [ ] Порт 8080 открыт и доступен
- [ ] Тестовый платеж прошел успешно
- [ ] Натальная карта генерируется после оплаты

## 📞 Если ничего не помогло

1. Проверьте логи бота на наличие ошибок
2. Проверьте, что все переменные окружения установлены
3. Проверьте настройки прокси/маршрутизации
4. Используйте резервные механизмы (периодическая проверка или `/start payment_success`)

## 💡 Важно знать

### Резервные механизмы работают БЕЗ webhook

Даже если webhook не работает, бот все равно может обработать платеж:

1. **Периодическая проверка** - каждые 2 минуты бот проверяет pending платежи
2. **Проверка при возврате** - когда пользователь возвращается после оплаты (`/start payment_success`)

НО: Webhook быстрее и надежнее - платеж обрабатывается сразу после оплаты!

### Процесс генерации натальной карты

После успешной оплаты:
1. Бот помечает пользователя как оплатившего (`has_paid = 1`)
2. Проверяет, заполнен ли профиль (имя, дата, время, место)
3. Если профиль заполнен → автоматически генерирует натальную карту
4. Если профиль не заполнен → предлагает заполнить профиль

**Важно**: Для генерации натальной карты нужен полностью заполненный профиль!

