# 🔄 Обновление кода на сервере

## Быстрый способ (рекомендуется)

### Вариант 1: Использование скрипта

1. **Подключитесь к серверу:**
   ```bash
   ssh root@ваш_ip_адрес
   # или
   ssh astralbot@ваш_ip_адрес
   ```

2. **Перейдите в директорию бота:**
   ```bash
   cd ~/Astral_Bot
   # или
   cd /home/astralbot/Astral_Bot
   ```

3. **Запустите скрипт обновления:**
   ```bash
   ./update_server.sh
   ```

   Если скрипта нет на сервере, скопируйте его:
   ```bash
   # На вашем локальном компьютере (из директории проекта)
   scp update_server.sh root@ваш_ip:/home/astralbot/Astral_Bot/
   
   # Затем на сервере
   chmod +x update_server.sh
   ./update_server.sh
   ```

### Вариант 2: Ручное обновление

1. **Подключитесь к серверу:**
   ```bash
   ssh root@ваш_ip_адрес
   ```

2. **Переключитесь на пользователя бота (если используется):**
   ```bash
   su - astralbot
   ```

3. **Перейдите в директорию бота:**
   ```bash
   cd ~/Astral_Bot
   ```

4. **Получите обновления:**
   ```bash
   git pull origin main
   ```

5. **Обновите зависимости (если изменились):**
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt
   ```

6. **Перезапустите бота:**
   ```bash
   exit  # вернуться в root, если нужно
   sudo systemctl restart astral-bot
   ```

7. **Проверьте статус:**
   ```bash
   sudo systemctl status astral-bot
   ```

## Для разных платформ

### VPS (Timeweb, DigitalOcean и т.д.)

См. **Вариант 2: Ручное обновление** выше.

### Railway

Railway автоматически обновляется при push в GitHub! Просто:

```bash
# На локальном компьютере
git push origin main
```

Railway автоматически перезапустит бота с новым кодом.

### Render

Render также автоматически обновляется при push в GitHub. Если нужно обновить вручную:

1. Зайдите в панель Render
2. Выберите ваш сервис
3. Нажмите "Manual Deploy" → "Deploy latest commit"

## Проверка обновления

После обновления проверьте:

1. **Статус бота:**
   ```bash
   sudo systemctl status astral-bot
   ```

2. **Логи бота:**
   ```bash
   sudo journalctl -u astral-bot -f
   ```

   Должно быть:
   - ✅ Нет ошибок
   - ✅ Webhook успешно установлен
   - ✅ Бот готов к работе

3. **Webhook статус:**
   ```bash
   cd ~/Astral_Bot
   python check_bot_status.py
   ```

4. **Проверка в Telegram:**
   - Отправьте боту команду `/start`
   - Бот должен ответить

## Решение проблем

### Ошибка: "git pull" не работает

```bash
# Проверьте настройки git
git remote -v

# Если репозиторий не настроен, добавьте remote:
git remote add origin https://github.com/NikRoshchin1894/astral-bot.git

# Затем повторите pull
git pull origin main
```

### Ошибка: Конфликт при обновлении

```bash
# Сохраните локальные изменения
git stash

# Получите обновления
git pull origin main

# Примените сохраненные изменения (если нужно)
git stash pop
```

### Бот не запускается после обновления

1. Проверьте логи:
   ```bash
   sudo journalctl -u astral-bot -n 100
   ```

2. Проверьте переменные окружения:
   ```bash
   cd ~/Astral_Bot
   source venv/bin/activate
   python -c "from dotenv import load_dotenv; import os; load_dotenv(); print('Token:', bool(os.getenv('TELEGRAM_BOT_TOKEN')))"
   ```

3. Проверьте зависимости:
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt
   ```

### Откат к предыдущей версии

Если что-то пошло не так, можно откатиться:

```bash
cd ~/Astral_Bot
git log --oneline -10  # посмотреть последние коммиты
git reset --hard <commit_hash>  # откатиться к конкретному коммиту
sudo systemctl restart astral-bot
```

## Автоматическое обновление (опционально)

Можно настроить автоматическое обновление через cron:

```bash
# Откройте crontab
crontab -e

# Добавьте строку (обновление каждый день в 3:00 ночи)
0 3 * * * cd /home/astralbot/Astral_Bot && /home/astralbot/Astral_Bot/update_server.sh >> /home/astralbot/Astral_Bot/logs/update.log 2>&1
```

---

**После обновления бот должен работать с последними исправлениями!** ✅

