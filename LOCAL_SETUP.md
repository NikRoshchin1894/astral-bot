# Локальный запуск бота 🤖

## Быстрая инструкция

### 1. Получение токенов

**Telegram Bot Token:**
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

**OpenAI API Key (опционально):**
1. Зарегистрируйтесь на https://platform.openai.com/
2. Получите API ключ
3. Добавьте способ оплаты (функция платная)

### 2. Создание файла .env

Создайте файл `.env` в корне проекта:

```bash
nano .env
```

Добавьте следующие строки:

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
OPENAI_API_KEY=ваш_openai_ключ_или_оставьте_пустым
```

**Важно:** Функция натальной карты работает только с OpenAI API. Остальные функции работают без него.

### 3. Запуск бота

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите бота
python bot.py
```

### 4. Тестирование

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Попробуйте все функции:
   - Знаки зодиака
   - Гороскоп
   - Совместимость
   - Фазы Луны
   - Натальная карта (если API ключ настроен)

## Остановка бота

Нажмите `Ctrl+C` в терминале

## Решение проблем

**Бот не отвечает:**
- Проверьте правильность токена в `.env`
- Проверьте интернет-соединение
- Посмотрите логи в терминале

**Ошибка с OpenAI:**
- Бот работает и без OpenAI API
- Функция натальной карты не будет работать без ключа
- Остальные функции работают нормально

**Ошибка импорта:**
```bash
source venv/bin/activate
pip install -r requirements.txt
```

## Что дальше?

После успешного локального тестирования смотрите инструкции в `README.md` для деплоя на облачный сервер.



