# 🔄 Режимы работы бота

## 📋 Описание

Бот поддерживает два режима работы:

### 1. 🌐 Режим WEBHOOK (для продакшена)

**Когда используется:**
- Когда установлена переменная окружения `TELEGRAM_WEBHOOK_URL`
- Для продакшена на сервере
- Когда нужна мгновенная обработка обновлений

**Как работает:**
- Telegram отправляет обновления на ваш сервер через webhook
- Обновления обрабатываются мгновенно
- Требует публичный URL с HTTPS

**Преимущества:**
- ✅ Мгновенная обработка обновлений
- ✅ Эффективное использование ресурсов
- ✅ Подходит для продакшена

---

### 2. 🔄 Режим POLLING (для разработки/тестирования)

**Когда используется:**
- Когда `TELEGRAM_WEBHOOK_URL` НЕ установлен
- Для локальной разработки
- Для тестирования на локальной машине

**Как работает:**
- Бот периодически запрашивает обновления у Telegram
- Не требует публичного URL
- Работает на любой машине

**Преимущества:**
- ✅ Не требует публичного URL
- ✅ Проще для разработки
- ✅ Работает за NAT/firewall

---

## ⚙️ Настройка

### Для WEBHOOK режима:

```bash
export TELEGRAM_WEBHOOK_URL="https://your-domain.com/webhook/telegram"
```

### Для POLLING режима:

Просто не устанавливайте `TELEGRAM_WEBHOOK_URL` или удалите его из `.env`

---

## 🔍 Как определить текущий режим

При запуске бот выводит в лог:

**WEBHOOK:**
```
🌐 Запуск бота в режиме WEBHOOK
```

**POLLING:**
```
🔄 Запуск бота в режиме POLLING
💡 Для продакшена установите TELEGRAM_WEBHOOK_URL
```

---

## ⚠️ Важно

1. **Не используйте оба режима одновременно!**
   - Если установлен `TELEGRAM_WEBHOOK_URL`, используется webhook
   - Если не установлен, используется polling

2. **Для продакшена используйте WEBHOOK**
   - Polling не подходит для продакшена
   - Webhook более эффективен и надежен

3. **При переключении режимов:**
   - При переходе с webhook на polling - старый webhook автоматически удаляется
   - При переходе с polling на webhook - webhook устанавливается автоматически

---

## 🛠️ Примеры использования

### Локальная разработка (POLLING):

```bash
# Не устанавливайте TELEGRAM_WEBHOOK_URL
python bot.py
```

### Продакшен (WEBHOOK):

```bash
# Установите TELEGRAM_WEBHOOK_URL
export TELEGRAM_WEBHOOK_URL="https://your-domain.com/webhook/telegram"
python bot.py
```

---

**Итог:** Polling режим восстановлен и доступен для разработки/тестирования! 🎉

