# 🔄 Инструкция по перезапуску бота

## ⚠️ ВАЖНО

Я не могу напрямую перезапустить бота на вашем сервере. Вам нужно выполнить команды на сервере самостоятельно.

## 🚀 Быстрый перезапуск

### Вариант 1: Через systemd (рекомендуется)

**Подключитесь к серверу и выполните:**

```bash
sudo systemctl restart astral-bot
```

**Проверка статуса:**
```bash
sudo systemctl status astral-bot
```

**Просмотр логов:**
```bash
sudo journalctl -u astral-bot -n 50 -f
```

### Вариант 2: Вручную (если не используется systemd)

**1. Остановить бота:**
```bash
# Найти процесс
ps aux | grep bot.py

# Остановить (замените PID на реальный)
kill <PID>
# или остановить все процессы бота:
pkill -f bot.py
```

**2. Запустить бота:**
```bash
cd /path/to/Astral_Bot
source venv/bin/activate  # если используете venv
python bot.py
```

**Или в screen/tmux:**
```bash
screen -S bot
cd /path/to/Astral_Bot
python bot.py
# Нажмите Ctrl+A, затем D для отключения
```

## 📋 После перезапуска

### 1. Подождать 30 секунд

Дайте боту время запуститься и установить webhook.

### 2. Проверить статус (локально или на сервере)

```bash
python check_bot_status.py
```

**Ожидаемый результат:**
```
✅ Webhook УСТАНОВЛЕН
URL: https://nikroshchin1894-astral-bot-e77d.twc1.net/webhook/telegram
```

### 3. Проверить все webhook

```bash
python check_all_webhooks.py
```

**Ожидаемый результат:**
```
✅ Telegram webhook: Работает корректно
✅ YooKassa webhook: Работает корректно
```

## 🔍 Что должно быть в логах после перезапуска

После перезапуска в логах должно появиться:

```
🌐 Запуск webhook сервера на 0.0.0.0:8080
   📱 Telegram webhook: /webhook/telegram
   💳 YooKassa webhook: /webhook/yookassa
✅ Webhook сервер запущен в отдельном потоке
🌐 Запуск бота в режиме WEBHOOK
🔗 Установка webhook в Telegram...
✅ Webhook успешно установлен в Telegram
✅ Бот запущен в режиме WEBHOOK и готов к работе!
```

**НЕ должно быть:**
```
🔄 Запуск бота в режиме POLLING
```

## ✅ Чек-лист

- [ ] Подключился к серверу
- [ ] Перезапустил бота (`sudo systemctl restart astral-bot`)
- [ ] Подождал 30 секунд
- [ ] Проверил статус (`python check_bot_status.py`)
- [ ] Webhook установлен ✅

## 💡 Если что-то пошло не так

### Бот не запускается

1. Проверьте логи:
   ```bash
   sudo journalctl -u astral-bot -n 100
   ```

2. Проверьте, что все переменные окружения установлены:
   ```bash
   cat .env | grep -E "TELEGRAM|YOOKASSA|DATABASE"
   ```

3. Проверьте, что порт 8080 свободен:
   ```bash
   netstat -tuln | grep 8080
   ```

### Webhook не устанавливается

1. Убедитесь, что `TELEGRAM_WEBHOOK_URL` установлен в `.env`
2. Проверьте логи на ошибки
3. Попробуйте установить webhook вручную:
   ```bash
   python setup_telegram_webhook.py
   ```

---

**После перезапуска бота webhook установится автоматически!** 🚀

