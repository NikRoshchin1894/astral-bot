# 📊 Отчет о статусе Webhook

Дата проверки: Создан после проверки webhook

## 🔍 Результаты проверки

### 1. Telegram Webhook
- ❌ **Статус**: Не установлен в Telegram
- ✅ **URL настроен**: `https://nikroshchin1894-astral-bot-e77d.twc1.net/webhook/telegram`
- ⚠️ **Проблема**: Webhook URL настроен в .env, но не зарегистрирован в Telegram API

### 2. YooKassa Webhook
- ❌ **Статус**: Недоступен (502 Bad Gateway)
- ✅ **URL настроен**: `https://nikroshchin1894-astral-bot-e77d.twc1.net/webhook/yookassa`
- ✅ **Credentials настроены**: Shop ID и Secret Key установлены
- ⚠️ **Проблема**: Сервер webhook не запущен или недоступен

## 🔧 Причины проблем

### 502 Bad Gateway означает:
1. **Бот не запущен на сервере** - самый вероятный вариант
2. **Webhook сервер не запустился** - проблема при инициализации
3. **Проблема с прокси/маршрутизацией** в Timeweb Cloud
4. **Порт 8080 не доступен** извне

### Telegram webhook не установлен:
- Бот не запущен, поэтому webhook не был установлен автоматически
- Или бот запущен в режиме polling вместо webhook

## ✅ Решения

### Шаг 1: Проверить, запущен ли бот на сервере

Подключитесь к серверу и проверьте:

```bash
# Проверка процесса бота
ps aux | grep bot.py

# Проверка systemd сервиса (если используется)
sudo systemctl status astral-bot

# Проверка логов
sudo journalctl -u astral-bot -n 50 --no-pager
# или
tail -n 50 bot.log
```

### Шаг 2: Запустить бот, если он не запущен

```bash
# Если используется systemd
sudo systemctl start astral-bot
sudo systemctl enable astral-bot

# Или вручную
cd /path/to/Astral_Bot
source venv/bin/activate
python bot.py
```

### Шаг 3: Проверить логи бота

После запуска бота должны быть следующие логи:

```
🌐 Запуск webhook сервера на 0.0.0.0:8080
   📱 Telegram webhook: /webhook/telegram
   💳 YooKassa webhook: /webhook/yookassa
🔗 Установка webhook в Telegram...
✅ Webhook успешно установлен в Telegram
✅ Бот запущен в режиме WEBHOOK и готов к работе!
```

### Шаг 4: Проверить настройки прокси в Timeweb Cloud

Убедитесь, что в настройках вашего приложения в Timeweb Cloud:

- Запросы к `/webhook/telegram` проксируются на `localhost:8080`
- Запросы к `/webhook/yookassa` проксируются на `localhost:8080`
- Порт 8080 открыт и доступен

### Шаг 5: Повторно проверить webhook

После запуска бота, запустите проверку:

```bash
python check_all_webhooks.py
```

## 📝 Чек-лист

- [ ] Бот запущен на сервере
- [ ] В логах есть сообщение о запуске webhook сервера
- [ ] Telegram webhook установлен (проверка через `check_all_webhooks.py`)
- [ ] YooKassa webhook доступен (HTTP 200 или другой успешный код)
- [ ] Webhook зарегистрирован в личном кабинете YooKassa

## 🔗 Полезные команды

```bash
# Полная проверка всех webhook
python check_all_webhooks.py

# Проверка статуса бота в Telegram
python check_bot_status.py

# Детальная диагностика YooKassa webhook
python diagnose_webhook.py

# Проверка настройки YooKassa webhook
python check_webhook_setup.py
```

## 📞 Если проблема не решена

1. Проверьте логи бота на наличие ошибок
2. Убедитесь, что все переменные окружения установлены правильно
3. Проверьте настройки прокси/маршрутизации в Timeweb Cloud
4. Убедитесь, что порт 8080 открыт и доступен извне

