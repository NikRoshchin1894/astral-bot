# Исправление проблемы с Docker Health Check

## Проблема

Docker контейнер останавливается на этапе проверки здоровья приложения. Flask не отвечает на проверку здоровья на порту 8080.

## Причины

1. Flask сервер не запускается вовремя
2. Health check endpoint не доступен на корневом пути `/`
3. Приложение падает до запуска Flask
4. Порты не правильно настроены

## Решения

### 1. Добавлен health check endpoint на корневой путь `/`

Теперь доступны два endpoint:
- `/` - корневой путь с информацией о сервисе
- `/health` - простой health check

### 2. Улучшен запуск Flask сервера

Flask теперь запускается в отдельном потоке сразу после создания, до инициализации Application.

### 3. Добавлен HEALTHCHECK в Dockerfile

Dockerfile теперь содержит:
```dockerfile
HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
  CMD curl -f http://localhost:8080/health || exit 1
```

Это дает приложению 40 секунд на запуск перед первой проверкой.

### 4. Проверка порта

Приложение автоматически определяет порт:
- Переменная `PORT` (для платформ типа Railway/Render)
- Переменная `WEBHOOK_PORT` 
- По умолчанию `8080`

## Переменные окружения

Убедитесь, что установлены:
- `TELEGRAM_WEBHOOK_URL` - для Telegram webhook
- `YOOKASSA_WEBHOOK_URL` - для YooKassa webhook (опционально)
- `PORT` или `WEBHOOK_PORT` - порт для Flask (по умолчанию 8080)

## Проверка после исправления

1. **Проверьте логи контейнера:**
   ```bash
   docker logs <container_id>
   ```

   Должно быть:
   ```
   🌐 Запуск webhook сервера на 0.0.0.0:8080
   ✅ Webhook сервер запущен в отдельном потоке
   ```

2. **Проверьте health check вручную:**
   ```bash
   curl http://localhost:8080/health
   # Должен вернуть: {"status":"ok"}
   
   curl http://localhost:8080/
   # Должен вернуть информацию о сервисе
   ```

3. **Проверьте статус контейнера:**
   ```bash
   docker ps
   # В колонке STATUS должно быть "healthy" или "starting"
   ```

## Дополнительные улучшения

Если проблема сохраняется:

1. **Увеличьте start-period в HEALTHCHECK:**
   ```dockerfile
   HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=5
   ```

2. **Используйте gunicorn вместо встроенного Flask сервера:**
   
   Создайте файл `gunicorn_config.py`:
   ```python
   bind = "0.0.0.0:8080"
   workers = 2
   threads = 4
   timeout = 120
   ```
   
   И измените CMD в Dockerfile:
   ```dockerfile
   CMD ["gunicorn", "--config", "gunicorn_config.py", "bot:app"]
   ```

3. **Проверьте логи на ошибки:**
   ```bash
   docker logs -f <container_id>
   ```

## Важно

- Flask должен запускаться **до** инициализации Application
- Health check endpoint должен отвечать **сразу** после запуска Flask
- Порты должны быть правильно экспортированы в Dockerfile

---

**После применения этих исправлений health check должен работать корректно!** ✅

