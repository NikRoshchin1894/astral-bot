# 🗄️ Настройка PostgreSQL на Railway

## Проблема
При каждом новом деплое на Railway данные пользователей терялись, так как SQLite хранит данные в локальных файлах, которые удаляются при перезапуске контейнера.

## Решение
Миграция на PostgreSQL - постоянное хранилище данных, которое сохраняется между деплоями.

## 📋 Шаги настройки на Railway

### 1. Добавьте PostgreSQL сервис
1. Откройте ваш проект на Railway: https://railway.app/dashboard
2. В вашем проекте нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст PostgreSQL базу данных

### 2. Настройте переменные окружения
Railway автоматически создаст переменную окружения `DATABASE_URL` в вашем worker сервисе. Она будет выглядеть примерно так:
```
postgresql://postgres:password@containers-us-west-xxx.railway.app:5432/railway
```

**Важно:** Если переменная не появилась автоматически:
1. Откройте настройки вашего worker сервиса
2. Перейдите в раздел **"Variables"**
3. Найдите `DATABASE_URL` в разделе **"Referenced"** (она создается автоматически при подключении PostgreSQL)
4. Если её нет, проверьте, что PostgreSQL сервис добавлен в тот же проект

### 3. Деплой
1. Railway автоматически задеплоит изменения из GitHub
2. При первом запуске бот автоматически создаст необходимые таблицы
3. Проверьте логи, чтобы убедиться, что подключение к БД успешно

### 4. Проверка работы
После деплоя проверьте:
- ✅ В логах должно быть: `База данных инициализирована (postgresql)`
- ✅ Данные пользователей сохраняются между деплоями
- ✅ Нет ошибок подключения к БД

## 🔧 Локальная разработка

Для локальной разработки ничего менять не нужно:
- Если `DATABASE_URL` не установлена, бот автоматически использует SQLite (`users.db`)
- Все функции работают одинаково с обеими БД

## 📊 Что было изменено

1. **Добавлена универсальная функция подключения** `get_db_connection()`:
   - Определяет тип БД (PostgreSQL или SQLite)
   - Возвращает соединение и тип БД

2. **Обновлены все функции работы с БД**:
   - `init_db()` - создание таблиц
   - `save_user_profile()` - сохранение профиля
   - `load_user_profile()` - загрузка профиля
   - `log_event()` - логирование событий
   - `user_has_paid()` - проверка оплаты
   - `mark_user_paid()` - отметка об оплате
   - `reset_user_payment()` - сброс оплаты

3. **Добавлена зависимость** `psycopg2-binary==2.9.9` в `requirements.txt`

## ⚠️ Важные моменты

- **PostgreSQL на Railway**: Все данные будут сохраняться между деплоями
- **SQLite локально**: Для разработки используется локальный файл `users.db`
- **Автоматическое определение**: Бот сам определяет, какую БД использовать
- **Бэкапы**: Railway автоматически делает бэкапы PostgreSQL

## 🆘 Если что-то не работает

1. **Проверьте переменную окружения**:
   - Убедитесь, что `DATABASE_URL` установлена в worker сервисе
   - Проверьте, что PostgreSQL сервис добавлен в проект

2. **Проверьте логи**:
   - Ищите ошибки подключения к БД
   - Убедитесь, что таблицы созданы

3. **Перезапустите сервис**:
   - В Railway Dashboard нажмите "Restart" на worker сервисе

---

**Готово! Теперь данные пользователей будут сохраняться при каждом деплое! 🎉**
