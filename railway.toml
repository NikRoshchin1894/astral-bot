[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python3 bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = ""
healthcheckTimeout = 100

# Ограничиваем количество реплик для предотвращения конфликтов
[deploy.healthcheck]
timeout = 100
interval = 30

