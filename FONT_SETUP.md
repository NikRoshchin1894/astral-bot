# 🔤 Настройка шрифта для PDF

## Проблема
Если в PDF вместо текста отображаются прямоугольники, это означает, что шрифт не поддерживает кириллицу.

## Решение

### Вариант 1: Добавить DejaVu Sans (Рекомендуется)

1. **Скачайте шрифт DejaVu Sans:**
   - Перейдите на https://dejavu-fonts.github.io/
   - Скачайте `DejaVuSans.ttf` (или полный пакет DejaVu Fonts)

2. **Добавьте шрифт в проект:**
   ```bash
   # Убедитесь, что папка fonts существует
   mkdir -p fonts
   
   # Скопируйте DejaVuSans.ttf в папку fonts/
   cp /path/to/DejaVuSans.ttf fonts/DejaVuSans.ttf
   ```

3. **Добавьте в git:**
   ```bash
   git add fonts/DejaVuSans.ttf
   git commit -m "Добавлен шрифт DejaVu Sans для поддержки кириллицы в PDF"
   git push
   ```

4. **Проверьте:**
   - После деплоя на Railway шрифт будет доступен
   - В логах должно появиться: `✅ Шрифт зарегистрирован: fonts/DejaVuSans.ttf`

### Вариант 2: Использовать системный шрифт (только для локальной разработки)

На macOS можно использовать Arial Unicode, но на Railway (Linux) его нет.

### Вариант 3: Автоматическая загрузка шрифта (для Railway)

Можно добавить код, который скачивает шрифт при запуске, но это замедлит старт бота.

## Проверка

После добавления шрифта:
1. Перезапустите бота на Railway
2. Проверьте логи - должно быть: `✅ Шрифт зарегистрирован`
3. Сгенерируйте тестовую натальную карту
4. Проверьте PDF - текст должен отображаться корректно

## Альтернативные шрифты

Если DejaVu Sans не подходит, можно использовать:
- **Noto Sans** (Google Fonts) - отличная поддержка кириллицы
- **Liberation Sans** - бесплатный аналог Arial
- **Roboto** - современный шрифт от Google

Все эти шрифты бесплатны и поддерживают кириллицу.

---

**Важно:** Шрифт должен быть в формате `.ttf` и находиться в папке `fonts/` проекта.
