# ⚡ Быстрое обновление сервера

## 🚀 Самый простой способ

### Если у вас есть SSH доступ к серверу:

Выполните эти команды на сервере:

```bash
# 1. Подключитесь к серверу (на вашем компьютере)
ssh root@ваш_сервер_ip
# или
ssh astralbot@ваш_сервер_ip

# 2. Перейдите в директорию бота
cd ~/Astral_Bot
# или
cd /home/astralbot/Astral_Bot

# 3. Обновите код
git pull origin main

# 4. Перезапустите бота
sudo systemctl restart astral-bot

# 5. Проверьте статус
sudo systemctl status astral-bot
```

**Готово!** Бот обновлен и перезапущен.

---

## 📋 Подробные инструкции

### Способ 1: Использование скрипта (рекомендуется)

1. **Скопируйте скрипт на сервер** (с вашего компьютера):
   ```bash
   scp update_server.sh root@ваш_сервер_ip:/tmp/
   ```

2. **На сервере:**
   ```bash
   ssh root@ваш_сервер_ip
   mv /tmp/update_server.sh ~/Astral_Bot/
   cd ~/Astral_Bot
   chmod +x update_server.sh
   ./update_server.sh
   ```

### Способ 2: Ручное обновление (3 команды)

```bash
cd ~/Astral_Bot && git pull origin main && sudo systemctl restart astral-bot
```

### Способ 3: Для Railway/Render (автоматически)

Если бот развернут на Railway или Render:
- Просто сделайте `git push origin main` — обновление произойдет автоматически!

---

## ✅ Проверка после обновления

```bash
# Проверьте логи
sudo journalctl -u astral-bot -f

# Проверьте webhook
cd ~/Astral_Bot
python check_bot_status.py

# Проверьте в Telegram
# Отправьте боту /start
```

---

## 🔧 Если что-то пошло не так

### Бот не запускается:

```bash
# Посмотрите логи
sudo journalctl -u astral-bot -n 50

# Проверьте переменные окружения
cd ~/Astral_Bot
source venv/bin/activate
python -c "from dotenv import load_dotenv; import os; load_dotenv(); print('Token:', bool(os.getenv('TELEGRAM_BOT_TOKEN')))"
```

### Нужна помощь?

См. полную инструкцию в файле: `ОБНОВЛЕНИЕ_СЕРВЕРА.md`

---

**После обновления все исправления будут применены!** ✅

