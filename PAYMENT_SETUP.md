# Настройка тестовых платежей 💳

## Получение тестового токена провайдера

Для работы тестовых платежей в Telegram боте необходимо получить тестовый токен провайдера:

### Шаг 1: Настройка платежей в @BotFather

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/mybots`
3. Выберите вашего бота из списка
4. Выберите `Payments` (Платежи)
5. Выберите `Test` для получения тестового токена
6. Скопируйте полученный токен

### Шаг 2: Добавление токена в переменные окружения

Добавьте токен в переменные окружения вашего проекта:

**Для локальной разработки:**
Создайте или отредактируйте файл `.env`:
```
TELEGRAM_PROVIDER_TOKEN=ваш_тестовый_токен_от_BotFather
```

**Для Railway:**
1. Откройте ваш проект в Railway
2. Перейдите в раздел Variables
3. Добавьте новую переменную:
   - Key: `TELEGRAM_PROVIDER_TOKEN`
   - Value: ваш тестовый токен от BotFather

### Шаг 3: Проверка настройки

После добавления токена:
1. Перезапустите бота (если он запущен)
2. Попробуйте создать тестовый платеж
3. Проверьте логи бота - должна появиться строка "Используется provider_token для создания invoice"

## Важные замечания

- **Тестовый токен** используется только для тестирования платежей без реального списания средств
- **Продакшн токен** нужно получать через официальный платежный провайдер (Stripe, YooKassa и т.д.)
- Тестовые платежи работают только в тестовом режиме Telegram Payments

## Устранение проблем

**Проблема: "Настройка оплаты не завершена"**
- Проверьте, что переменная `TELEGRAM_PROVIDER_TOKEN` установлена
- Убедитесь, что токен скопирован полностью, без пробелов
- Перезапустите бота после добавления переменной

**Проблема: Платеж не проходит**
- Проверьте логи бота на наличие ошибок
- Убедитесь, что платежи включены в настройках бота через @BotFather
- Проверьте, что используется правильный тестовый токен (не продакшн)

## Дополнительная информация

Для получения продакшн токена провайдера необходимо:
1. Зарегистрироваться у платежного провайдера (YooKassa, Stripe и т.д.)
2. Настроить интеграцию в @BotFather
3. Получить продакшн токен
4. Добавить его в переменные окружения

