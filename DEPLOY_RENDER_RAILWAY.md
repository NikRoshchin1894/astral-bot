# Развертывание бота на Render / Railway

## Обзор платформ

### Render
- ✅ Бесплатный план доступен
- ✅ Автоматическое развертывание из Git
- ✅ Простая настройка через веб-интерфейс
- ⚠️ Бесплатные сервисы засыпают после 15 минут неактивности

### Railway
- ✅ Бесплатный план с $5 кредитами
- ✅ Автоматическое развертывание из Git
- ✅ Очень простая настройка
- ✅ Не засыпает на бесплатном плане

---

# 🚀 Развертывание на Render

## Шаг 1: Подготовка репозитория

### 1.1 Создайте Git репозиторий
```bash
cd /Users/nsroschin/Documents/Astral_Bot
git init
git add .
git commit -m "Initial commit"
```

### 1.2 Создайте репозиторий на GitHub
1. Зайдите на https://github.com
2. Создайте новый репозиторий (например, `astral-bot`)
3. Не добавляйте README, .gitignore или лицензию (они уже есть)

### 1.3 Загрузите код на GitHub
```bash
git remote add origin https://github.com/YOUR_USERNAME/astral-bot.git
git branch -M main
git push -u origin main
```

## Шаг 2: Создание необходимых файлов для Render

### 2.1 Создайте Procfile
Создайте файл `Procfile` (без расширения) в корне проекта:

```
worker: python3 bot.py
```

### 2.2 Создайте runtime.txt (опционально)
Укажите версию Python:

```
python-3.11.0
```

### 2.3 Убедитесь, что .env в .gitignore
Файл `.env` уже должен быть в `.gitignore` (не загружайте токены в Git!)

## Шаг 3: Развертывание на Render

### 3.1 Регистрация
1. Зайдите на https://render.com
2. Зарегистрируйтесь через GitHub
3. Подтвердите email

### 3.2 Создание нового сервиса
1. Нажмите **"New +"** → **"Background Worker"**
2. Подключите ваш GitHub репозиторий
3. Выберите репозиторий `astral-bot`

### 3.3 Настройка сервиса
- **Name**: `astral-bot` (или любое другое имя)
- **Environment**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python3 bot.py`
- **Plan**: выберите **Free** (или платный план)

### 3.4 Настройка переменных окружения
В разделе **"Environment"** добавьте:
- `TELEGRAM_BOT_TOKEN` = ваш токен Telegram бота
- `OPENAI_API_KEY` = ваш ключ OpenAI API

### 3.5 Создание сервиса
1. Нажмите **"Create Background Worker"**
2. Render автоматически начнет развертывание
3. Дождитесь завершения (обычно 2-5 минут)

### 3.6 Проверка работы
1. Откройте вкладку **"Logs"**
2. Должны увидеть: `Бот запущен!`
3. Проверьте бота в Telegram

## Шаг 4: Мониторинг и обновления

### 4.1 Просмотр логов
- Войдите в Render Dashboard
- Выберите ваш сервис
- Откройте вкладку **"Logs"**

### 4.2 Обновление бота
1. Внесите изменения в код локально
2. Загрузите изменения на GitHub:
   ```bash
   git add .
   git commit -m "Update bot"
   git push
   ```
3. Render автоматически развернет новую версию

### 4.3 Перезапуск сервиса
- В Render Dashboard → ваш сервис → **"Manual Deploy"** → **"Clear build cache & deploy"**

---

# 🚂 Развертывание на Railway

## Шаг 1: Подготовка репозитория

Выполните шаги 1.1-1.3 из раздела Render (создание Git репозитория на GitHub).

## Шаг 2: Создание необходимых файлов для Railway

### 2.1 Создайте Procfile
```
worker: python3 bot.py
```

### 2.2 Создайте railway.json (опционально)
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python3 bot.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

## Шаг 3: Развертывание на Railway

### 3.1 Регистрация
1. Зайдите на https://railway.app
2. Нажмите **"Start a New Project"**
3. Войдите через GitHub

### 3.2 Создание проекта
1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите репозиторий `astral-bot`
4. Railway автоматически обнаружит Python проект

### 3.3 Настройка переменных окружения
1. Откройте ваш проект в Railway
2. Перейдите в **"Variables"**
3. Добавьте переменные:
   - `TELEGRAM_BOT_TOKEN` = ваш токен Telegram бота
   - `OPENAI_API_KEY` = ваш ключ OpenAI API

### 3.4 Настройка сервиса
1. Откройте сервис (он создастся автоматически)
2. В разделе **"Settings"** → **"Start Command"**:
   - Убедитесь, что указано: `python3 bot.py`
3. Railway автоматически установит зависимости из `requirements.txt`

### 3.5 Развертывание
1. Railway автоматически начнет развертывание
2. Откройте вкладку **"Deployments"** для отслеживания прогресса
3. Дождитесь статуса **"Success"**

### 3.6 Проверка работы
1. Откройте вкладку **"Logs"**
2. Должны увидеть: `Бот запущен!`
3. Проверьте бота в Telegram

## Шаг 4: Мониторинг и обновления

### 4.1 Просмотр логов
- Войдите в Railway Dashboard
- Выберите ваш проект
- Откройте вкладку **"Logs"**

### 4.2 Обновление бота
1. Внесите изменения в код локально
2. Загрузите изменения на GitHub:
   ```bash
   git add .
   git commit -m "Update bot"
   git push
   ```
3. Railway автоматически развернет новую версию

### 4.3 Перезапуск сервиса
- В Railway Dashboard → ваш проект → **"Deployments"** → нажмите **"Redeploy"**

---

# 📋 Сравнение платформ

| Характеристика | Render | Railway |
|---------------|--------|---------|
| Бесплатный план | ✅ Да (засыпает) | ✅ Да ($5 кредитов) |
| Автозапуск после сна | ✅ Да | N/A (не засыпает) |
| Простота настройки | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Скорость развертывания | Быстро | Очень быстро |
| Логи | ✅ Да | ✅ Да |
| Мониторинг | Базовый | Продвинутый |
| Домен | ✅ Да | ✅ Да |
| SSL сертификат | ✅ Да | ✅ Да |

---

# ⚠️ Важные моменты

## Безопасность
- ❌ **НИКОГДА** не загружайте `.env` файл в Git
- ✅ Используйте переменные окружения на платформе
- ✅ `.env` уже в `.gitignore`

## Render - Бесплатный план
- Сервисы засыпают после 15 минут неактивности
- Первый запрос после пробуждения может быть медленным (холодный старт)
- Для production рекомендуется платный план ($7/месяц)

## Railway - Бесплатный план
- $5 кредитов в месяц (обычно хватает для небольшого бота)
- Сервис не засыпает
- После исчерпания кредитов нужно будет пополнить баланс или выбрать платный план

## Рекомендации
- Для тестирования: используйте Railway (не засыпает)
- Для production с небольшой нагрузкой: Railway или Render платный план
- Для production с большой нагрузкой: VPS (см. `DEPLOYMENT.md`)

---

# 🔧 Решение проблем

## Бот не запускается

### Render
1. Проверьте логи в Render Dashboard
2. Убедитесь, что переменные окружения установлены
3. Проверьте, что `Procfile` создан правильно
4. Убедитесь, что команда запуска: `python3 bot.py`

### Railway
1. Проверьте логи в Railway Dashboard
2. Убедитесь, что переменные окружения установлены
3. Проверьте, что `Procfile` создан правильно
4. В Settings → Start Command должно быть: `python3 bot.py`

## Ошибка "Module not found"
1. Убедитесь, что все зависимости в `requirements.txt`
2. Проверьте, что версии Python совместимы
3. Перезапустите развертывание

## Бот не отвечает
1. Проверьте логи на наличие ошибок
2. Убедитесь, что токены правильные
3. Проверьте, что бот запущен (статус в Dashboard)

---

# 📚 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Railway Documentation](https://docs.railway.app)
- [Python on Render](https://render.com/docs/python)
- [Railway Python Guide](https://docs.railway.app/develop/python)

---

# 🎉 Готово!

После развертывания ваш бот будет работать 24/7 на облачной платформе. Любые изменения, которые вы загрузите в GitHub, автоматически развернутся на платформе.

