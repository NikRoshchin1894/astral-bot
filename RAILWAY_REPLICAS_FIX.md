# 🔧 Как настроить 1 реплику в Railway

## ⚠️ Важно о Railway Worker сервисах

В Railway для **worker** сервисов (Background Worker) настройка реплик **не отображается в UI**. По умолчанию worker должен запускаться как один инстанс.

Проблема обычно в том, что:
1. Запущено несколько **deployment'ов** одновременно
2. Старые инстансы не были остановлены
3. Railway автоматически создал несколько инстансов

## ✅ Решение 1: Проверьте и удалите лишние deployment'ы

### Шаг 1: Откройте раздел Deployments

1. В Railway Dashboard откройте ваш проект
2. Выберите сервис `worker`
3. В правом сайдбаре нажмите **"Deploy"** (видно на вашем скриншоте)
4. Или откройте вкладку **"Deployments"** вверху

### Шаг 2: Проверьте активные deployment'ы

1. Посмотрите список всех deployment'ов
2. Если видите несколько активных (зеленый статус) - это проблема!
3. Должен быть активным только **один** последний deployment

### Шаг 3: Удалите старые deployment'ы

1. Найдите старые deployment'ы (не последний)
2. Нажмите на три точки (⋯) рядом с deployment'ом
3. Выберите **"Delete"** или **"Stop"**
4. Оставьте активным только **последний** deployment

## ✅ Решение 2: Используйте Railway CLI

### Установите Railway CLI

```bash
npm i -g @railway/cli
```

### Войдите в Railway

```bash
railway login
```

### Привяжите проект

```bash
cd /Users/nsroschin/Documents/Astral_Bot
railway link
```

### Ограничьте масштабирование

```bash
# Проверьте текущее состояние
railway status

# Ограничьте до 1 инстанса (для worker это обычно по умолчанию)
railway scale 1
```

## ✅ Решение 3: Пересоздайте сервис

Если ничего не помогает:

1. **Удалите текущий сервис:**
   - В Railway Dashboard
   - Выберите сервис `worker`
   - В правом сайдбаре нажмите **"Danger"**
   - Нажмите **"Delete Service"**

2. **Создайте новый сервис:**
   - В проекте нажмите **"New"** → **"GitHub Repo"**
   - Выберите репозиторий `astral-bot`
   - Railway автоматически определит тип как **Worker**
   - Добавьте переменные окружения:
     - `TELEGRAM_BOT_TOKEN`
     - `OPENAI_API_KEY`

3. **Проверьте:**
   - Должен создаться только один инстанс
   - Проверьте логи

## ✅ Решение 4: Используйте render.yaml (для Render)

Если проблема не решается на Railway, попробуйте **Render**:

1. Зайдите на https://render.com
2. Создайте новый **Background Worker**
3. Подключите репозиторий `astral-bot`
4. В настройках найдите **"Instance Count"**
5. Установите **Instance Count = 1**
6. Разверните бота

## 🔍 Как проверить, сколько инстансов запущено

### В Railway:

1. Откройте **Deployments**
2. Посчитайте активные deployment'ы (зеленый статус)
3. Должен быть только **1 активный**

### В логах:

Если видите несколько записей типа:
```
Бот запущен!
Попытка запуска 1/3...
Бот запущен!
Попытка запуска 1/3...
```
Это означает, что запущено несколько инстансов.

## 📋 Чек-лист

- [ ] Проверен раздел Deployments
- [ ] Удалены старые deployment'ы
- [ ] Активен только 1 deployment
- [ ] Сервис перезапущен
- [ ] В логах нет ошибок Conflict
- [ ] Бот отвечает в Telegram

## 🆘 Если ничего не помогает

1. **Полностью удалите проект на Railway**
2. **Создайте новый проект**
3. **Создайте новый сервис Worker**
4. **Настройте переменные окружения**
5. **Разверните бота**

Railway для worker сервисов должен автоматически использовать только 1 инстанс. Если проблема сохраняется, это может быть баг Railway - в таком случае лучше использовать Render, где настройка инстансов более явная.

---

**Подсказка:** На вашем скриншоте нажмите на **"Deploy"** в правом сайдбаре - там должны быть настройки deployment'ов!

