# Астрологический Telegram Бот 🌟

Этот бот предоставляет информацию о знаках зодиака, гороскопы, проверку совместимости и информацию о фазах Луны.

## Возможности

- 🔮 Подробная информация о всех 12 знаках зодиака
- ✨ Ежедневные гороскопы
- 💫 Проверка совместимости знаков зодиака
- 🌙 Информация о фазах Луны и их влиянии
- 📜 Создание персональной натальной карты с помощью ChatGPT
- 💬 Интерактивное меню с кнопками

## Установка и настройка

### 1. Получение токенов

**Telegram Bot Token:**
1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и создайте бота
4. Сохраните полученный токен (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

**OpenAI API Key:**
1. Зарегистрируйтесь на [OpenAI](https://platform.openai.com/)
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Сохраните его (начинается с `sk-...`)

### 2. Облачное развертывание (рекомендуется)

#### 🚀 Быстрый старт на Render/Railway

1. **Подготовьте код:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/astral-bot.git
   git push -u origin main
   ```

2. **Выберите платформу:**
   - **Railway** (рекомендуется): https://railway.app
   - **Render**: https://render.com

3. **Развертывание:**
   - См. подробные инструкции в `QUICK_DEPLOY.md` или `DEPLOY_RENDER_RAILWAY.md`

**Преимущества облачного развертывания:**
- ✅ Работает 24/7
- ✅ Автоматическое обновление при push в Git
- ✅ Встроенный мониторинг и логи
- ✅ Бесплатные планы доступны

### 3. Локальная установка (для тестирования)

```bash
# Клонируйте репозиторий или скопируйте файлы
git clone <your-repo-url>
cd Astral_Bot

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Для Linux/Mac
# или
venv\Scripts\activate  # Для Windows

# Установите зависимости
pip install -r requirements.txt

# Создайте файл .env с вашими токенами
cat > .env << EOF
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
EOF

# Запустите бота
python bot.py
```

## Деплой на облачный сервер

### Вариант 1: DigitalOcean Droplet (Рекомендуется)

1. **Создайте Droplet:**
   - Зайдите на [DigitalOcean](https://www.digitalocean.com/)
   - Создайте Ubuntu 22.04 Droplet (минимум 1GB RAM)
   - Выберите план ($6/месяц - достаточно)

2. **Подключитесь к серверу:**
   ```bash
   ssh root@your_server_ip
   ```

3. **Обновите систему:**
   ```bash
   apt update && apt upgrade -y
   ```

4. **Установите Python и необходимые пакеты:**
   ```bash
   apt install python3 python3-pip python3-venv git -y
   ```

5. **Создайте пользователя для бота (рекомендуется):**
   ```bash
   adduser telegrambot
   usermod -aG sudo telegrambot
   su - telegrambot
   ```

6. **Клонируйте проект или загрузите файлы:**
   ```bash
   mkdir -p ~/astral_bot
   cd ~/astral_bot
   # Загрузите файлы через SFTP или используйте git
   ```

7. **Создайте виртуальное окружение и установите зависимости:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

8. **Создайте файл с переменными окружения:**
   ```bash
   nano .env
   ```
   Добавьте:
   ```
   TELEGRAM_BOT_TOKEN=your_actual_bot_token_here
   OPENAI_API_KEY=your_openai_api_key_here
   ```

9. **Создайте systemd service для автозапуска:**
   ```bash
   sudo nano /etc/systemd/system/astral-bot.service
   ```
   
   Добавьте следующее содержимое:
   ```ini
   [Unit]
   Description=Astral Telegram Bot
   After=network.target

   [Service]
   Type=simple
   User=telegrambot
   WorkingDirectory=/home/telegrambot/astral_bot
   Environment="PATH=/home/telegrambot/astral_bot/venv/bin"
   ExecStart=/home/telegrambot/astral_bot/venv/bin/python bot.py
   Restart=always
   RestartSec=10

   [Install]
   WantedBy=multi-user.target
   ```

10. **Запустите сервис:**
    ```bash
    sudo systemctl daemon-reload
    sudo systemctl enable astral-bot
    sudo systemctl start astral-bot
    ```

11. **Проверьте статус:**
    ```bash
    sudo systemctl status astral-bot
    ```

12. **Просмотр логов:**
    ```bash
    sudo journalctl -u astral-bot -f
    ```

### Вариант 2: Heroku (Проще, но платно)

1. **Установите Heroku CLI:**
   ```bash
   # Для Linux
   curl https://cli-assets.heroku.com/install.sh | sh
   ```

2. **Войдите в Heroku:**
   ```bash
   heroku login
   ```

3. **Создайте приложение:**
   ```bash
   heroku create your-app-name
   ```

4. **Установите переменную окружения:**
   ```bash
   heroku config:set TELEGRAM_BOT_TOKEN=your_token_here
   ```

5. **Создайте Procfile:**
   ```bash
   echo "worker: python bot.py" > Procfile
   ```

6. **Деплой:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   heroku git:remote -a your-app-name
   git push heroku main
   ```

7. **Добавьте Worker Dyno (обязательно!):**
   ```bash
   heroku ps:scale worker=1
   ```

### Вариант 3: Railway (Новый и простой)

1. Зайдите на [Railway.app](https://railway.app/)
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Добавьте переменную окружения `TELEGRAM_BOT_TOKEN`
6. Railway автоматически определит Python проект и развернет его

### Вариант 4: Render.com (Бесплатный вариант)

1. Зайдите на [Render.com](https://render.com/)
2. Создайте новый "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - Name: `astral-bot`
   - Environment: `Python 3`
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python bot.py`
   - Add Environment Variable: `TELEGRAM_BOT_TOKEN` = `your_token`

### Вариант 5: PythonAnywhere (Простой для начинающих)

1. Зарегистрируйтесь на [PythonAnywhere.com](https://www.pythonanywhere.com/) (free tier)
2. Откройте Bash консоль
3. Клонируйте проект:
   ```bash
   git clone <your-repo-url>
   cd Astral_Bot
   ```
4. Установите зависимости:
   ```bash
   pip3.10 install --user -r requirements.txt
   ```
5. Создайте `env.py`:
   ```python
   import os
   os.environ['TELEGRAM_BOT_TOKEN'] = 'your_token_here'
   ```
6. Измените начало `bot.py`:
   ```python
   import env  # Добавьте эту строку
   import logging
   # ... остальной код
   ```
7. Создайте scheduled task для запуска бота:
   - В веб-интерфейсе PythonAnywhere
   - Tasks → Schedule a new task
   - Command: `python3.10 /home/yourusername/Astral_Bot/bot.py`
   - Every day at: 00:00 (можно выбрать любое время)

## Управление ботом

### Остановка бота:
```bash
sudo systemctl stop astral-bot
```

### Запуск бота:
```bash
sudo systemctl start astral-bot
```

### Перезапуск после изменений:
```bash
sudo systemctl restart astral-bot
```

### Просмотр логов:
```bash
sudo journalctl -u astral-bot -f
```

## Структура проекта

```
Astral_Bot/
├── bot.py              # Основной файл бота
├── requirements.txt    # Зависимости Python
├── env.example         # Пример файла с переменными
├── README.md           # Документация
└── .env                # Файл с токеном (создается вручную)
```

## Функция натальной карты

Бот может создавать персональные натальные карты с помощью ChatGPT. Для этого:

1. Пользователь нажимает кнопку "📜 Натальная карта" в главном меню
2. Вводит свои данные:
   - Имя
   - Дата рождения (ДД.ММ.ГГГГ)
   - Время рождения (ЧЧ:ММ)
   - Место рождения (город, страна)
3. Бот генерирует детальную астрологическую натальную карту

**Важно:** Для работы этой функции необходим API ключ OpenAI (не бесплатно).

### Настройка OpenAI API:

1. Зарегистрируйтесь на https://platform.openai.com/
2. Добавьте способ оплаты
3. Получите API ключ в разделе API Keys
4. Добавьте ключ в файл `.env` как `OPENAI_API_KEY`

**Стоимость:** Примерно $0.01-0.02 за одну натальную карту (зависит от модели GPT)

## Решение проблем

### Бот не отвечает:
1. Проверьте, что токен правильный
2. Убедитесь, что бот запущен: `systemctl status astral-bot`
3. Проверьте логи: `journalctl -u astral-bot -n 50`

### Ошибки при установке:
- Убедитесь, что используется Python 3.8+
- Проверьте интернет-соединение
- Попробуйте обновить pip: `pip install --upgrade pip`

### Бот падает сразу после запуска:
- Проверьте логи на наличие ошибок
- Убедитесь, что токен установлен правильно
- Проверьте права доступа к файлам

## Обновление бота

```bash
cd ~/astral_bot
git pull  # Или обновите файлы вручную
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart astral-bot
```

## Безопасность

- **НЕ ДЕЛИТЕСЬ** вашим токеном с кем-либо
- Храните `.env` в `.gitignore` (если используете git)
- Не коммитьте токен в репозиторий
- Регулярно обновляйте зависимости

## Лицензия

MIT License

## Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.
