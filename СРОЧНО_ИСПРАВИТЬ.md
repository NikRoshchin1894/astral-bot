# ⚡ СРОЧНО: Что сделать ПРЯМО СЕЙЧАС

## ❌ Проблема: Ошибка 409 Conflict

Бот не может работать из-за конфликта между webhook и polling.

## ✅ Решение (выберите ОДИН вариант)

### Вариант А: Использовать Webhook (для продакшена) ⭐ РЕКОМЕНДУЕТСЯ

**Шаг 1:** Запустить бот на сервере (webhook автоматически установится)

```bash
# Подключитесь к серверу Timeweb Cloud и выполните:
sudo systemctl start astral-bot
# или запустите вручную
cd /path/to/Astral_Bot && python bot.py
```

**Шаг 2:** Проверить, что webhook установлен

```bash
python check_bot_status.py
```

**Готово!** Бот будет работать в webhook режиме, оплата обработается автоматически.

---

### Вариант Б: Использовать Polling (для разработки)

**Шаг 1:** Удалить webhook

```bash
python remove_webhook.py
```

**Шаг 2:** Остановить все процессы бота

```bash
# Найти процессы
ps aux | grep bot.py

# Остановить все
pkill -f bot.py
# или
sudo systemctl stop astral-bot
```

**Шаг 3:** Подождать 1 минуту

**Шаг 4:** Запустить бота заново

```bash
python bot.py
```

**Готово!** Бот будет работать в polling режиме, оплата обработается через 2 минуты.

---

## 🔍 Диагностика

Выполните:

```bash
python fix_telegram_conflict.py
```

Скрипт покажет, что именно нужно сделать.

---

## 💳 После исправления конфликта

1. Проверьте готовность оплаты:
   ```bash
   python check_payment_readiness.py
   ```

2. Зарегистрируйте webhook в YooKassa (если используете webhook режим):
   - [yookassa.ru/my](https://yookassa.ru/my) → Настройки → Уведомления
   - URL: `https://nikroshchin1894-astral-bot-e77d.twc1.net/webhook/yookassa`

3. Протестируйте оплату в боте

---

## ⚠️ ВАЖНО

- Используйте ТОЛЬКО ОДИН режим: webhook ИЛИ polling
- Если `TELEGRAM_WEBHOOK_URL` установлен → используйте webhook
- Если `TELEGRAM_WEBHOOK_URL` НЕ установлен → используйте polling

---

📖 Подробные инструкции: `FIX_CONFLICT_AND_PAYMENT.md`

