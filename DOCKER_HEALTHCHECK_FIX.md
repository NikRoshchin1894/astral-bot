# 🔧 Исправление проблемы с Docker Health Check

## ❌ Проблема

Контейнер останавливается на этапе проверки здоровья приложения:
- Flask не отвечает на проверку здоровья на порту 8080
- Health check endpoint недоступен
- Приложение может падать до запуска Flask

## ✅ Решения

### 1. Добавлен health check endpoint на корневой путь `/`

Теперь доступны два endpoint:
- **`/`** - корневой путь с информацией о сервисе и статусом
- **`/health`** - простой health check endpoint

### 2. Улучшен запуск Flask сервера

- Flask запускается в отдельном потоке сразу после создания
- Логирование улучшено для отладки
- Добавлена информация о доступных endpoints

### 3. Добавлен HEALTHCHECK в Dockerfile

```dockerfile
HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
  CMD curl -f http://localhost:8080/health || exit 1
```

Это дает приложению **40 секунд** на запуск перед первой проверкой.

### 4. Проверка порта

Приложение автоматически определяет порт в порядке приоритета:
1. Переменная `PORT` (для платформ типа Railway/Render)
2. Переменная `WEBHOOK_PORT`
3. По умолчанию `8080`

## 📋 Переменные окружения

Убедитесь, что установлены:

```bash
TELEGRAM_WEBHOOK_URL=https://your-domain.com/webhook/telegram
YOOKASSA_WEBHOOK_URL=https://your-domain.com/webhook/yookassa  # опционально
PORT=8080  # или WEBHOOK_PORT=8080
```

## 🔍 Проверка после исправления

### 1. Проверьте логи контейнера

```bash
docker logs <container_id>
```

Должно быть:
```
🌐 Запуск webhook сервера на 0.0.0.0:8080
   📱 Telegram webhook: /webhook/telegram
   💳 YooKassa webhook: /webhook/yookassa
   ❤️  Health check: / и /health
✅ Webhook сервер запущен в отдельном потоке
```

### 2. Проверьте health check вручную

```bash
# Изнутри контейнера или с хоста
curl http://localhost:8080/health
# Должен вернуть: {"status":"ok"}

curl http://localhost:8080/
# Должен вернуть информацию о сервисе
```

### 3. Проверьте статус контейнера

```bash
docker ps
# В колонке STATUS должно быть "healthy" или "starting (health: starting)"
```

## 🛠️ Дополнительные улучшения

Если проблема сохраняется:

### Увеличьте start-period в HEALTHCHECK

В `Dockerfile`:
```dockerfile
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=5 \
  CMD curl -f http://localhost:8080/health || exit 1
```

### Используйте gunicorn для production

1. Убедитесь, что `gunicorn` в `requirements.txt`
2. Создайте файл `gunicorn_config.py`:
   ```python
   bind = "0.0.0.0:8080"
   workers = 2
   threads = 4
   timeout = 120
   keepalive = 5
   ```
3. Измените запуск в `bot.py` (для production)

### Проверьте лимиты ресурсов

В `docker-compose.yml` или при запуске контейнера:
```yaml
deploy:
  resources:
    limits:
      memory: 1G  # Увеличьте, если не хватает
    reservations:
      memory: 512M
```

## ⚠️ Важно

1. **Flask должен запускаться ДО инициализации Application**
   - Flask запускается в отдельном потоке сразу
   - Не блокирует основной поток

2. **Health check endpoint должен отвечать СРАЗУ после запуска Flask**
   - Endpoint `/health` доступен сразу
   - Endpoint `/` также доступен

3. **Порты должны быть правильно экспортированы**
   - `EXPOSE 8080` в Dockerfile
   - Порт 8080 доступен извне

## 📝 Что было исправлено

1. ✅ Добавлен endpoint `/` для health check
2. ✅ Улучшен запуск Flask сервера
3. ✅ Добавлен HEALTHCHECK в Dockerfile
4. ✅ Улучшено логирование
5. ✅ Добавлена документация

---

**После применения этих исправлений health check должен работать корректно!** ✅

